<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DigiteX Passport Login</title>
  <style>
    @font-face {
      font-family: 'digitfont';
      src: url('/digifont.ttf') format('truetype');
    }
    @import url('https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@100;400;600&display=swap');

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body, html {
      height: 100%;
      font-family: "Be Vietnam Pro", sans-serif;
      background: url('login.jpg') no-repeat center center/cover;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #ffffff;
    }

    .digifont {
      font-family: 'digitfont', sans-serif;
      font-size: 2em;
      letter-spacing: 2px;
      color: #4971ed; /* passport gold */
      text-shadow: 0 0 10px rgba(73, 113, 237, 0.3);
    }

    .login-container {
      background: rgba(0, 0, 0, 0.65);
      border: 1px solid rgba(73, 113, 237, 0.3);
      border-radius: 12px;
      backdrop-filter: blur(12px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.6);
      padding: 40px 30px;
      width: 100%;
      max-width: 380px;
      text-align: center;
      animation: fadeIn 1s ease-in-out;
    }

    .login-container img {
      width: 90px;
      margin-bottom: 10px;
    }

    hr {
      border: 0.5px solid #277ca3;
      margin: 15px 0;
    }

    .subtitle {
      font-size: 0.85em;
      color: #aaa;
      margin-bottom: 20px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    input {
      padding: 12px;
      border-radius: 8px;
      border: 1px solid #444;
      background: #d1d1d1;
      color: #000000;
      font-size: 0.95em;
      transition: all 0.3s ease;
      outline: none;
    }

    input::placeholder {
      color: #888;
    }

    input:focus {
      border-color: #4971ed;
      box-shadow: 0 0 8px #15a38c;
    }

    button {
      padding: 12px;
      border-radius: 8px;
      border: none;
      background: linear-gradient(135deg, #4971ed, #1b6c92);
      font-weight: bold;
      font-size: 1em;
      cursor: pointer;
      transition: all 0.3s ease;
      color: #ffffff;
    } 

    button:hover {
      background: linear-gradient(135deg, #4971ed, #277ca3);
      transform: scale(1.05);
      box-shadow: 0 4px 15px rgba(141, 141, 141, 0.5);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Responsive */
    @media (max-width: 500px) {
      .login-container { 
        width: 90%;
        padding: 30px 20px;
      }
      .digifont {
        font-size: 1.6em;
      }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <img src="logo.png" alt="DigiteX Logo"><br>
    <span class="digifont">DigiteX Passport</span>
    <hr>
    <p class="subtitle">
      Login with your DigiteX Passport account to access the Dashboard.
    </p>
    <form action="/login" method="post">
      <input type="text" name="username" placeholder="Username" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">Login</button>
    </form>
  </div>
</body>
</html>
