<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Digitex Passport</title>
  <style>
@import url('https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;600;700&display=swap');
@font-face {
    font-family: 'digitfont';
    src: url('./digifont.ttf') format('truetype');
  }
  html {
    background-color: #000000;
    margin: 0;
    padding: 15px;
    color: #ffffff;
    font-family: "Be Vietnam Pro", sans-serif;
    font-weight: 100;
    font-size: 1.2em;
    background: url('resources/login.jpg');
    background-size: cover;
  }
  .passport-login{
    height: 600px;
    width: 30%;
    padding: 20px;
    margin: auto;
    background-color: #000000c4;
    -webkit-backdrop-filter: blur(5px);
    backdrop-filter: blur(5px);
    border: #097db3 2px solid;
    border-top: 10px solid #097db3;
    border-radius: 15px;
  }
  .introt{
    font-size: 1.5em;
    font-weight: 600;
    position: relative;
    text-align: center;
    color: #ffffff;
    user-select: none;
  }
  
  .login-form {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 25px;
    margin-top: 30px;
    padding: 20px;
  }
  
  .form-group {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  
  .form-input {
    width: 100%;
    padding: 15px 20px;
    border-radius: 10px;
    border: 2px solid #333;
    background-color: rgba(255, 255, 255, 0.1);
    color: #ffffff;
    font-size: 1em;
    font-family: "Be Vietnam Pro", sans-serif;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    box-sizing: border-box;
  }
  
  .form-input:focus {
    outline: none;
    border-color: #097db3;
    background-color: rgba(255, 255, 255, 0.15);
    box-shadow: 0 0 0 3px rgba(9, 125, 179, 0.2);
  }
  
  .form-input::placeholder {
    color: rgba(255, 255, 255, 0.7);
    font-weight: 400;
  }
  
  .login-button {
    width: 70%;
    padding: 15px 20px;
    border-radius: 10px;
    border: none;
    background: linear-gradient(135deg, #097db3, #0a9dd9);
    color: white;
    font-size: 1.1em;
    font-weight: 600;
    font-family: "Be Vietnam Pro", sans-serif;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-top: 10px;
  }
  
  .login-button:hover {
    background: linear-gradient(135deg, #0a9dd9, #097db3);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(9, 125, 179, 0.4);
  }
  .back{
    font-family: sans-serif;
    font-size: 1.5em;
    padding: 10px;
    background-color: #1372a9a6;
    -webkit-backdrop-filter: blur(5px);
    backdrop-filter: blur(5px);
    border-radius: 600px;
    width: fit-content;
    padding: 5px 15px;
    cursor: pointer;
  }
  </style>
</head>
<body>
  <div class="back" onclick="javascript:history.back()">< Back</div>
  <div class="passport-login">
    <center><span class="introt">Administrator <br> DigiteX Passport Login</span></center>
    <img src="resources/logo.png" alt="DigiteX Logo" style="display: block; margin: 20px auto; height: 150px;">
     
    <form action="/login" method="POST" class="login-form">
      <div class="form-group">
        <input type="text" name="username" placeholder="Username" required class="form-input">
      </div>
      <div class="form-group">
        <input type="password" name="password" placeholder="Password" required class="form-input">
      </div>
      <button type="submit" class="login-button">Login</button>
    </form>
  </div>
</body>
</html>